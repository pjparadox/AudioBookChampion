from setuptools import setup, find_packages

setup(
    name='audio_bchampion',  # all lowercase
    version='0.1',
    packages=find_packages(),  # will pick up the 'audio_bchampion' folder
)
